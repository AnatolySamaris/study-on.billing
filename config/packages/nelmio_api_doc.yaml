nelmio_api_doc:
    documentation:
        info:
            title: StudyOn.Billing documentation
            description: There's a docs page for the service StudyOn.Billing
            version: 1.0.0
        components:
            securitySchemes:
                bearerAuth:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
